Do inherited

# Enter your initialization code here
Calculate irConference as prConference
Calculate ilData as plData

Calculate icReportTitle as con('Attendee count for each arrangement ',' - ',irConference.CF_TITLE)
Calculate icReportSubTitle as ''
Calculate icSortTitle as con('sorted by ',prSortOption.sortName)

Calculate ilData.$line as 0
While $Loop(ilData)
	Do $cinst.$printrecord()
End While