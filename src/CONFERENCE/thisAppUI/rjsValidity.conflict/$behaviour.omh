# called from a containing form
Calculate icMode as pcMode

If $cinst.$container ## should be rjsMenuApp
	Do $cinst.$container().$behaviour(icMode)
End If

Calculate $cinst.$objs.TabControl_VAL.$enabled as $cinst.icMode=''